export const missionVisuals = {
  // Mobile-first defaults. These only affect rendering; mission rules and timing stay unchanged.
  maxDestructionEffects: 16,
  smokeEnabled: true,
  maxSmokeWisps: 2,
  maxOrbAuras: 3,
  maxOrbTrails: 6,
  orbTrailIntervalMs: 160,
  orbVisualUpdateIntervalMs: 50,
  reducedMotion: true,
} as const
